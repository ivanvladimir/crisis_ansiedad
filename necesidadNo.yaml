conversations:
  - 'derivacion.yaml'
slots:
  - terminar
conversations:
  - 'derivacion.yaml'
strategies:
  terminar:
    - say "Entiendo 😊. ¿Continuamos con nuestra conversación?"
    - say "Escribe Si o No"
    - input continuar | yesno
  despedida:
    - say "De acuerdo, en mi menú está disponible el ejercicio de re-entrenamiento de la respiración 🫁, información sobre tus posibles síntomas y el directorio con teléfonos 📞 que te podrían ser de utilidad ahora o en los próximos días. Cuídate mucho y regresa cuando gustes {} 😊.".format(nombre)
script:
  - solve terminar
  - if continuar == True then solve despedida
  - if continuar == False then say "Okay!, sigamos entonces"
  - if continuar == False then solve derivacion
