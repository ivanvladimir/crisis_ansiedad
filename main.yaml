settings:
  name: SAM

conversations:
  - 'completa.yaml'
  - 'Bentilacion.yaml'
  - 'derivacion.yaml'
  - 'directorio.yaml'
  - 'psicoEducacion.yaml'
  - 'escuchaActiva.yaml'
  - 'necesidadSi.yaml'
  - 'necesidadNo.yaml'
  - 'categorizacion.yaml'
  - 'psicoEducacion.yaml'
  - 'necesidadSi.yaml'
  - 'necesidadNo.yaml'
  - 'respNo.yaml'
  - 'respSi.yaml'
slots:
  - opcion

strategies:
  menu:
    - say "Menu:"
    - say "1. Primeros auxilios psicologicos (intervencion completa)"
    - say "2. Bentilacion:re entrenamiento de la respiracion"
    - say "3. Derivacion:directorio"
    - say "4. psicoeducacion"
    - say "ingresa el numero de la opcion deseada"
    - input opcion | number
script:
  - solve menu
  - if opcion == 1 then solve completa
  - if opcion == 2 then solve Bentilacion
  - if opcion == 3 then solve derivacion
  - if opcion == 4 then solve psicoeducacion